---
version: 2

project_name: ajq

before:
  hooks:
    - go mod download

builds:
  - id: ajq
    main: ./cmd/ajq
    binary: ajq
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - >-
        -s -w
        -X {{ .ModulePath }}/internal/version.Version={{ .Version }}

archives:
  - id: ajq
    ids:
      - ajq
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64{{ else }}{{ .Arch }}{{ end }}
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - LICENSE
      - README.md

checksum:
  name_template: checksums.txt
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - "^ci:"


brews:
  - name: ajq
    ids:
      - ajq
    repository:
      owner: ricardocabral
      name: homebrew-ajq
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    directory: Formula
    skip_upload: '{{ if index .Env "HOMEBREW_TAP_GITHUB_TOKEN" }}false{{ else }}true{{ end }}'
    homepage: https://github.com/ricardocabral/ajq
    description: jq-like stream processor for semantic matching and extraction
    license: MIT
    test: |
      system "#{bin}/ajq", "--version"
    install: |
      bin.install "ajq"
    commit_author:
      name: github-actions[bot]
      email: 41898282+github-actions[bot]@users.noreply.github.com
    commit_msg_template: Homebrew formula update for {{ .ProjectName }} {{ .Tag }}

release:
  github:
    owner: ricardocabral
    name: ajq
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## ajq {{ .Tag }}

    Install a prebuilt binary from the assets below. Homebrew packaging and the
    checksum-verifying install script are generated by the release pipeline.

    Releases published after provenance attestations are enabled can be verified
    with the GitHub CLI:

    ```bash
    gh attestation verify <downloaded-archive-or-checksums.txt> --repo ricardocabral/ajq
    ```

    Checksum verification and provenance verification are separate steps; compare
    archive SHA-256 digests with `checksums.txt` in addition to any attestation
    verification you perform.
