{
  "name": "ajq-website",
  "version": "0.1.0",
  "description": "Documentation and marketing site for ajq, built with Hugo + Docsy.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "hugo --cleanDestinationDir --gc --minify",
    "serve": "hugo server"
  },
  "devDependencies": {
    "autoprefixer": "^10.6.0",
    "postcss": "^8.5.28",
    "postcss-cli": "^12.0.0"
  }
}
